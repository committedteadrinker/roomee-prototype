# roomee-prototype
A prototype for an app for booking rooms for meetings.
This was requested as part of a bigger project.

This app uses data that is generated by Google Calendar API.

Need to add: Caching meetings on local DB (To mitigate lost of connection).